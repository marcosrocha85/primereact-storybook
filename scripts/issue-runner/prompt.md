Implement only the selected GitHub issue in this repository. Read and follow AGENTS.md and any applicable nested instructions. The user explicitly selected this issue; do not substitute the next backlog item.

The calling shell has verified a clean main, updated it with pull --ff-only and created the dedicated branch shown above. These startup Git steps are already complete. Stay on that branch. Fetch the complete component-review queue, read the issue body/comments, dependencies and linked/open PRs. Stop with status blocked if there is an unresolved dependency, conflicting implementation, or missing acceptance criterion.

The shell owns delivery: it will stage exactly your reported files, commit, push, create the PR, wait for checks, squash via gh, confirm the merge and return to main. The user has authorized these steps. Do not stage, commit, switch/create/delete branches, push, create/edit/merge PRs, close issues, or otherwise write to GitHub yourself. Do not reset, clean, stash or discard work. Do not start another issue. Do not launch a second instance of this runner. Read-only gh queries are allowed. A task needing unavailable permissions must report blocked; do not bypass the sandbox or approval rules.

For component reviews:
- Inspect Button as the structural reference, the current component and generator, the Sakai UI Kit sources, and the installed PrimeReact types and implementation.
- Inventory ALL native properties, inherited attributes, nested models, events, templates and supported value modes. Record in the existing component evidence document which are exposed, passed through, adapted, not applicable, or intentionally outside the curated examples. Distinguish verified behavior from source inspection and untested combinations. Do not claim exhaustive testing from an inventory alone.
- Keep the patch scoped. Explain necessary behavior or visual changes beyond the explicit acceptance criteria in the PR body. Avoid adding unrelated visual features.
- Preserve the native contract for forwarded props. Do not silently narrow selection/value modes, drop supplied callbacks, or replace user pt customizations with defaults (or lose required defaults when a user supplies unrelated pt entries). Use focused tests for the combinations actually intercepted by the wrapper. Document any deliberate limitation explicitly.
- Preserve generation, Summary/Default, no Summary Controls, single Default instance, useArgs synchronization, copyable source, relative assets and localization rules.
- Inspect the result visually at desktop/mobile widths, including icon/text spacing, states and compositions. Add regression assertions for meaningful behavior. Test supplied callbacks, not only the wrapper's own feedback.

Run only the relevant tests required by AGENTS.md, plus npm run build, npm run build-storybook and git diff --check. Do not run the whole browser suite by default. Do not skip failing tests, weaken types, add suppressions or change unrelated infrastructure to manufacture a pass. Keep a record of the exact commands and actual results. Stop any servers/processes you started; preserve pre-existing ones.

Return the JSON required by the output schema. Use status ready only if every acceptance criterion and required validation is complete, there are no unresolved regressions/blockers, and the API review above is recorded (or explicitly not applicable for a non-component issue). Include an English PR title/body with the problem, final changes, scope decisions, API limitations and exact validation evidence. The shell appends the closing keyword. Do not close or reference completion of other issues. List every changed/new/deleted repository file in changed_files, using relative paths; for renames include both paths. Do not include generated ignored build artifacts. Never report tests as passed unless you ran them.

Recovery protocol:
- A failing regression or build is a technical task to investigate, not by itself an external blocker. Reproduce it, identify its cause and try a scoped correction before returning blocked.
- Before blaming PrimeReact, compare a minimal native reproduction with the wrapper and check whether the test expectation is valid. Do not remove a valid regression assertion to get green results.
- You may explicitly constrain a proven unsupported playground combination only when the acceptance criteria still hold. Document and test the limitation; never silently drop user callbacks or narrow native values. If satisfying the issue requires a scope decision, use blocker_kind scope.
- Use blocker_kind technical for an unresolved implementation/test failure, external for unavailable dependencies/services, permission for unavailable authorization, scope for required user decisions, and none only for ready results. Describe evidence, attempted fixes and the next hypothesis in recovery_notes.
- On subsequent attempts, inspect all earlier diagnostic.txt, validation.log, result.json and changes.patch files. Continue the existing changes and use new evidence; do not restart the issue or repeat an unchanged approach.
- The controller independently runs both builds, changed contract tests, generator checks when relevant, and affected component browser tests against a temporary static server. It inherits browser-library environment variables. It never executes shell command strings from your report.
