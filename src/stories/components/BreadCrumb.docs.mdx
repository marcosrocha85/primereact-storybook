import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./BreadCrumb.examples";
import exampleSource from "./BreadCrumb.examples.tsx?raw";

<Meta title="Components/BreadCrumb/Summary" />

<Title>BreadCrumb</Title>

<Subtitle>Contextual navigation through the Sakai page hierarchy. Demo commands report the selected destination; supply menu item URLs or commands for application navigation.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Without Home

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ showHome: false }} />
</div>

<Source code={exampleSource + '\n// Render without the Home item:\n<Example initialArgs={{ showHome: false }} />'} language="tsx" />

### Disabled item

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ model: [{ label: 'Computer' }, { label: 'Notebook', disabled: true }, { label: 'Item' }] }} />
</div>

<Source code={exampleSource + "\n// Disable an unavailable destination:\n<Example initialArgs={{ model: [{ label: 'Computer' }, { label: 'Notebook', disabled: true }, { label: 'Item' }] }} />"} language="tsx" />

## Playground

Open [Default](?path=/story/components-breadcrumb--default) to change properties with Controls. Examples on this page keep their own state.
