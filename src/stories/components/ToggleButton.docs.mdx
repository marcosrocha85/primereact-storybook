import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./ToggleButton.examples";
import exampleSource from "./ToggleButton.examples.tsx?raw";

<Meta title="Components/ToggleButton/Summary" />

<Title>ToggleButton</Title>

<Subtitle>On/off toggle button with labels, icons, and disabled or invalid states.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Icons and icon position

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<div className="flex flex-wrap gap-2">
  <Example initialArgs={{ onIcon: 'pi pi-check', offIcon: 'pi pi-times' }} />
  <Example initialArgs={{ iconPos: 'right' }} />
</div>
</div>

<Source code={"<div className=\"flex flex-wrap gap-2\">\n  <Example initialArgs={{ onIcon: 'pi pi-check', offIcon: 'pi pi-times' }} />\n  <Example initialArgs={{ iconPos: 'right' }} />\n</div>"} language="tsx" />

### Invalid and disabled states

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<div className="flex flex-wrap gap-2">
  <Example initialArgs={{ invalid: true }} />
  <Example initialArgs={{ disabled: true }} />
</div>
</div>

<Source code={"<div className=\"flex flex-wrap gap-2\">\n  <Example initialArgs={{ invalid: true }} />\n  <Example initialArgs={{ disabled: true }} />\n</div>"} language="tsx" />

## Playground

Open [Default](?path=/story/components-togglebutton--default) to change properties with Controls. Examples on this page keep their own state.
