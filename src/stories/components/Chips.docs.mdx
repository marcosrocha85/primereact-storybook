import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./Chips.examples";
import exampleSource from "./Chips.examples.tsx?raw";

<Meta title="Components/Chips/Summary" />

<Title>Chips</Title>

<Subtitle>Multi-value input rendered as chips.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Disabled

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ disabled: true }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ disabled: true }} />"} language="tsx" />

### Read only

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ value: ['Chicago', 'New York'], readOnly: true }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ value: ['Chicago', 'New York'], readOnly: true }} />"} language="tsx" />

### Invalid

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ value: ['Invalid'], invalid: true }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ value: ['Invalid'], invalid: true }} />"} language="tsx" />

### Filled

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ value: ['Piano', 'Keyboard'], variant: 'filled' }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ value: ['Piano', 'Keyboard'], variant: 'filled' }} />"} language="tsx" />

## Playground

Open [Default](?path=/story/components-chips--default) to change properties with Controls. Examples on this page keep their own state.
