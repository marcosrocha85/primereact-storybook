import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./FileUpload.examples";
import exampleSource from "./FileUpload.examples.tsx?raw";

<Meta title="Components/FileUpload/Summary" />

<Title>FileUpload</Title>

<Subtitle>File selection and upload queue with basic and advanced modes. The default playground simulates completion locally without a network request.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Basic mode

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ mode: 'basic', chooseLabel: 'Choose image' }} />
</div>

<Source code={"<Example initialArgs={{ mode: 'basic', chooseLabel: 'Choose image' }} />"} language="tsx" />

### Advanced multiple selection

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ mode: 'advanced', multiple: true, chooseLabel: 'Select images' }} />
</div>

<Source code={"<Example initialArgs={{ mode: 'advanced', multiple: true, chooseLabel: 'Select images' }} />"} language="tsx" />

### Automatic local upload

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ mode: 'advanced', auto: true, customUpload: true }} />
</div>

<Source code={"<Example initialArgs={{ mode: 'advanced', auto: true, customUpload: true }} />"} language="tsx" />

### Disabled state

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ disabled: true }} />
</div>

<Source code={"<Example initialArgs={{ disabled: true }} />"} language="tsx" />

## Playground

Open [Default](?path=/story/components-fileupload--default) to change properties with Controls. Examples on this page keep their own state.
