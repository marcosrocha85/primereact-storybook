import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Button } from "primereact/button";
import { Panel } from "primereact/panel";

<Meta title="Components/Panel/Summary" />

<Title>Panel</Title>

<Subtitle>
  Panel used by Sakai as a content container with a titled header and optional
  collapse toggle.
</Subtitle>

## Usage

<div className="component-example sb-unstyled" style={{ width: "30rem", maxWidth: "100%" }}>
  <Panel header="Project Details">
    <div className="m-0 line-height-3">
      Use a panel to group related content under a clear heading. The header
      keeps the section scannable while the body holds the detail.
    </div>
  </Panel>
</div>

<Source
  code={`<Panel header="Project Details">
  <p>
    Use a panel to group related content under a clear heading.
  </p>
</Panel>`}
  language="tsx"
/>

## Variations

### Toggleable

<div className="component-example sb-unstyled" style={{ width: "30rem", maxWidth: "100%" }}>
  <Panel header="Summary" toggleable>
    <div className="m-0 line-height-3">
      Add <code>toggleable</code> to let users collapse and expand the panel.
      Use this when content is supplementary and can be hidden without losing
      context.
    </div>
  </Panel>
</div>

<Source
  code={`<Panel header="Summary" toggleable>
  <p>
    Add toggleable to let users collapse and expand the panel.
  </p>
</Panel>`}
  language="tsx"
/>

### Collapsed by Default

<div className="component-example sb-unstyled" style={{ width: "30rem", maxWidth: "100%" }}>
  <Panel header="Advanced Settings" toggleable collapsed>
    <div className="m-0 line-height-3">
      Set <code>collapsed</code> together with <code>toggleable</code> to start
      the panel closed. Useful for secondary information that most users will
      not need immediately.
    </div>
  </Panel>
</div>

<Source
  code={`<Panel header="Advanced Settings" toggleable collapsed>
  <p>
    Set collapsed together with toggleable to start the panel closed.
  </p>
</Panel>`}
  language="tsx"
/>

### Custom Header

<div className="component-example sb-unstyled" style={{ width: "30rem", maxWidth: "100%" }}>
  <Panel
    header="Notifications"
    headerTemplate={(options) => {
      const {
        className,
        titleClassName,
        togglerClassName,
        togglerIconClassName,
        onTogglerClick,
      } = options;
      return (
        <div className={className}>
          <span className={titleClassName}>
            <i className="pi pi-bell mr-2"></i>
            Notifications
          </span>
          <div className="component-example sb-unstyled flex gap-2">
            <Button
              icon="pi pi-cog"
              text
              rounded
              severity="secondary"
              size="small"
              aria-label="Settings"
            />
            <button className={togglerClassName} onClick={onTogglerClick}>
              <span className={togglerIconClassName} />
            </button>
          </div>
        </div>
      );
    }}
    toggleable
  >
    <div className="m-0 line-height-3">
      Use <code>headerTemplate</code> to inject custom controls such as action
      buttons alongside the toggle affordance.
    </div>
  </Panel>
</div>

<Source
  code={`<Panel
  header="Notifications"
  headerTemplate={(options) => {
    const {
      className,
      titleClassName,
      togglerClassName,
      togglerIconClassName,
      onTogglerClick,
    } = options;

    return (
      <div className={className}>
        <span className={titleClassName}>
          <i className="pi pi-bell mr-2" />
          Notifications
        </span>
        <div className="component-example sb-unstyled flex gap-2">
          <Button icon="pi pi-cog" text rounded severity="secondary" size="small" aria-label="Settings" />
          <button className={togglerClassName} onClick={onTogglerClick}>
            <span className={togglerIconClassName} />
          </button>
        </div>
      </div>
    );
  }}
  toggleable
>
  <div>
    Use headerTemplate to inject custom controls such as action buttons alongside the toggle affordance.
  </div>
</Panel>`}
  language="tsx"
/>

## Playground

Open [Default](?path=/story/components-panel--default) to change properties with Controls. Examples on this page keep their own state.
