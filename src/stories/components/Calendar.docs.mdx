import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./Calendar.examples";
import exampleSource from "./Calendar.examples.tsx?raw";

<Meta title="Components/Calendar/Summary" />

<Title>Calendar</Title>

<Subtitle>Single date picker used in Sakai forms, with a popup, Today/Clear actions, floating labels, and validation states.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Floating label

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ floatLabel: true, showIcon: false, showButtonBar: false }} />
</div>

<Source code={exampleSource + '\n// Render a floating label:\n<Example initialArgs={{ floatLabel: true, showIcon: false, showButtonBar: false }} />'} language="tsx" />

### Invalid state

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ invalid: true }} />
</div>

<Source code={exampleSource + '\n// Render validation styling:\n<Example initialArgs={{ invalid: true }} />'} language="tsx" />

### Disabled

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ disabled: true }} />
</div>

<Source code={exampleSource + '\n// Render a disabled field:\n<Example initialArgs={{ disabled: true }} />'} language="tsx" />

## Playground

Open [Default](?path=/story/components-calendar--default) to change properties with Controls. Examples on this page keep their own state.
