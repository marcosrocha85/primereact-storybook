import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./ScrollTop.examples";
import exampleSource from "./ScrollTop.examples.tsx?raw";

<Meta title="Components/ScrollTop/Summary" />

<Title>ScrollTop</Title>

<Subtitle>Shortcut to scroll back to the top.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Custom icon

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ icon: 'pi pi-arrow-up' }} />
</div>

<Source code={exampleSource + "\n// Use a custom icon:\n<Example initialArgs={{ icon: 'pi pi-arrow-up' }} />"} language="tsx" />

### Automatic scroll behavior

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ behavior: 'auto' }} />
</div>

<Source code={exampleSource + "\n// Scroll to the top without animation:\n<Example initialArgs={{ behavior: 'auto' }} />"} language="tsx" />

### Lower visibility threshold

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ threshold: 20 }} />
</div>

<Source code={exampleSource + "\n// Show the control after a short scroll:\n<Example initialArgs={{ threshold: 20 }} />"} language="tsx" />

## Playground

Open [Default](?path=/story/components-scrolltop--default) to change properties with Controls. Examples on this page keep their own state.
