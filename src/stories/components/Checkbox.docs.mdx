import { Meta, Source, Subtitle, Title } from "@storybook/addon-docs/blocks";
import { Example } from "./Checkbox.examples";
import exampleSource from "./Checkbox.examples.tsx?raw";

<Meta title="Components/Checkbox/Summary" />

<Title>Checkbox</Title>

<Subtitle>Labeled checkbox for a boolean choice or an option in an application-managed selection.</Subtitle>

## Usage

<div className="component-example sb-unstyled"><Example /></div>

<Source code={exampleSource} language="tsx" />

## Variations

### Unchecked

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ checked: false }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ checked: false }} />"} language="tsx" />

### Disabled

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ disabled: true }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ disabled: true }} />"} language="tsx" />

### Read only

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ readOnly: true }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ readOnly: true }} />"} language="tsx" />

### Invalid

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ invalid: true, checked: false }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ invalid: true, checked: false }} />"} language="tsx" />

### Filled

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ variant: 'filled', checked: false }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ variant: 'filled', checked: false }} />"} language="tsx" />

### Custom icon

<div className="component-example sb-unstyled flex flex-wrap align-items-center gap-2">
<Example initialArgs={{ icon: 'pi pi-star-fill' }} />
</div>

<Source code={exampleSource + "\n// Render this variation:\n<Example initialArgs={{ icon: 'pi pi-star-fill' }} />"} language="tsx" />

## Playground

Open [Default](?path=/story/components-checkbox--default) to change properties with Controls. Examples on this page keep their own state.
